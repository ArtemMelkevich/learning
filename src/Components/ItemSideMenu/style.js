import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    textAlign: 'center',
  },
  container: {
    marginBottom: 10,
  },
  txt: {
    textAlign: 'center',
  },
});

export default styles;
